# NOTE: This file is maintained for convenience only.
# The canonical dependency list is in pyproject.toml.
# Preferred install: pip install -e ".[all]"

# Core dependencies
openai
python-dotenv
fire
httpx
rich
tenacity
prompt_toolkit
pyyaml
requests
jinja2
pydantic>=2.0
PyJWT[crypto]

# Web tools
firecrawl-py

# Image generation
fal-client

# mini-swe-agent dependencies (for terminal tool)
# Note: Install mini-swe-agent itself with: pip install -e ./mini-swe-agent
litellm>=1.75.5
typer
platformdirs

# Text-to-speech (Edge TTS is free, no API key needed)
edge-tts

# Optional: For cron expression parsing (cronjob scheduling)
croniter

# Optional: For messaging platform integrations (gateway)
python-telegram-bot>=20.0
discord.py>=2.1
aiohttp>=3.9.0

cryptography>=42.0.0
pynacl>=1.5.0
pyca-cryptography>=42.0.0

# async HTTP for IACR/arxiv paper fetching
httpx>=0.27.0
aiohttp>=3.9.0

# structured output parsing for research digests
jsonschema>=4.21.0
pydantic>=2.6.0

# scheduling
apscheduler>=3.10.0
